# Ejercicio 1

a = float(input("Entra el valor de 'a': "))
b = float(input("Entra el valor de 'b': "))
c = float(input("Entra el valor de 'c': "))

resultado = (a**3 * (b**2 - 2*a*c)) / (2 * b)

print(f"\nEl resultado es {resultado}.")